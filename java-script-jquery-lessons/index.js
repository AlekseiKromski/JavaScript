$(document).ready(function () {
    var header = $('.header');
    var a = $('<h2>Hello world</h2>');

    //Add a element to end of header elemenet
    header.append(a);
})

//Will call, when document will be ready
$(function (){

})